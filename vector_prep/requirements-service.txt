# Runtime dependencies for the retrieval service container only.
# CPU-only torch — the retrieval service embeds short query strings at
# request time (milliseconds on CPU); GPU is only needed for the bulk
# embed_context.py backfill, which runs locally, not in this container.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu

numpy<2
chromadb==0.5.0
sentence-transformers==3.0.1
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
structlog>=24.1.0
pydantic>=2.6.1
pydantic-settings>=2.4.0
